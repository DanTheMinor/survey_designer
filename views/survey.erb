<% if @survey.questions().any?() %>
  <h1>Here are the questions for <%= @survey.title() %></h1>
  <ul>
    <% @survey.questions().each() do |question| %>
      <li><a href='/questions/<%= question.id() %>/edit'><%= question.description() %></a></li>
    <% end %>
  </ul>
<% else %>
  <h1>There are no questions yet.</h1>
<% end %>

<form action="/surveys/<%=@survey.id()%>" method="post">
  <label for="description">Question description</label>
  <input id="description" name="description" type="text">

  <label for="answer">Correct answer (integers only please)</label>
  <input id="answer" name="answer" type="text">

  <input id="survey_id" type="hidden" name="survey_id" value="<%@survey.id%>">
  <button type="submit">Add Question</button>
</form>


<div class="container">
  <a href="/surveys">Back To All Surveys</a>
</div>
